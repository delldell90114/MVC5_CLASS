@model int?
@{
    var Credits = 9;
    //if (Model.HasValue)
    //{
    //    Credits = Model.Value;
    //}
}

@for (int i = 1; i <= Credits; i++)
{
    <label>
        @Html.RadioButton("", i, Model == i) @i
    </label>

}

@*--- Change to <Dropdownlist> practice --- *@
@*@{
        List<SelectListItem> ddl = new List<SelectListItem>();

        for (int i = 0; i < Model; i++)
        {
            //@("🤷‍♂️")

            ddl.Add(new SelectListItem { Text = i.ToString(), Value = i.ToString() });
        }
        ViewBag.dropdownlist = ddl;
    }

    @Html.DropDownList("dropdownlist")*@

